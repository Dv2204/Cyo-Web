import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    image: {
       height: '80vh',
      width: '37vw',
        paddingTop: '3.1rem'
    }
 }))