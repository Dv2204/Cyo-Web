import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    heading: {
        marginBottom: '0.5rem',
        marginLeft: '14rem',
        fontWeight:'bold',
        marginTop:'4rem'
    },
    headingicon:{
        marginLeft: '15rem',
        marginTop: '4rem',
        marginBottom: '0.5rem',
    },
    facheading: {
        textAlign:'center',
        marginTop: '2rem'
    }
 }))