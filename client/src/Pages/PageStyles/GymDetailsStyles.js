import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    heading: {
        marginTop: '1rem',
        marginBottom: '0.5rem',
        marginLeft: '17rem'
    }
 }))