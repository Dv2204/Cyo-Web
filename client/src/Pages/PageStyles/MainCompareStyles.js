import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
   top: {
       marginTop: '2rem'
   },
   body: {
       marginTop: '1rem'
   },
   line: {
       marginTop: '1rem'
   }
 }))