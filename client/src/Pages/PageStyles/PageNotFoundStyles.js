import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  error: {
    width: "100vw",
    height: "60vh ",
  },
  notfound: {
    color: "red",
    textAlign: "center",
    fontWeight: "bold",
  },
}));
